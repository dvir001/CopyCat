name: Build and push Rust service Docker images
on:
  push:
    paths:
    - 'crates/**'
    - '.dockerignore'
    - '.github/workflows/rust-docker.yml'
    - 'ci/Dockerfile.rust'
    - 'ci/rust-docker-target.sh'
    - 'Cargo.toml'
    - 'Cargo.lock'
    - 'rust-toolchain.toml'

jobs:
  build:
    name: 'rust docker build'
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
      - uses: docker/login-action@v3
        if: ${{ !env.ACT }}
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.CR_PAT }}

      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3

      # main docker build
      - run: echo "BRANCH_NAME=${GITHUB_REF#refs/heads/}" >> $GITHUB_ENV
      - uses: docker/build-push-action@v7
        with:
          # https://github.com/docker/build-push-action/issues/378
          context: .
          file: ci/Dockerfile.rust
          push: false 
          cache-from: type=registry,ref=ghcr.io/${{ github.repository_owner }}/docker-cache:rust
          cache-to: type=registry,ref=ghcr.io/${{ github.repository_owner }}/docker-cache:rust,mode=max
          outputs: .docker-bin

      # add more binaries here
      - run: |
          tag=${{ github.sha }} \
          branch=${{ env.BRANCH_NAME }} \
          push=$([ "${{ github.event_name }}" == "push" ] && echo true || echo false) \
          ci/rust-docker-target.sh
